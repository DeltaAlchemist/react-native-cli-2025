import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  userImageContainer: {
    borderWidth: 1,
    padding: 3,
    borderColor: '#f347a3ff',
  },
});

export default style;
